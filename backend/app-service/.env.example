# App Service Environment Variables

# Server Configuration
PORT=3002

# Database Configuration (same DB as auth-service)
DB_HOST=localhost
DB_PORT=5419
DB_NAME=TheNineteenthChamber
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false

# Environment
NODE_ENV=development
